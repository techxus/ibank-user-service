name: user-service -> local (feature branch)

on:
  push:
    branches:
      - "feature/**"

concurrency:
  group: user-service-local
  cancel-in-progress: true

jobs:
  deploy:
    uses: techxus/ibank-gitops-templates/.github/workflows/deploy.yml@master
    with:
      service_name: user-service
      docker_image: techxus/user-service
      gitops_repo: techxus/ibank-minikube-gitops
      gitops_branch: master
      values_file: env/local/user-service-values.yaml
      environment: local
      java_version: "17"
      maven_args: "-DskipTests"
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      GITOPS_REPO_PAT: ${{ secrets.GITOPS_REPO_PAT }}
